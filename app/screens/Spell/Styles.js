import { StyleSheet } from 'react-native';

const Styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        paddingTop: 50,
    },
    imageContainer: {
        borderRadius: 5,
        padding: 20,
    },
});

export default Styles;